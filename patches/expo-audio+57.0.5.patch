diff --git a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioRecorder.kt b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioRecorder.kt
index 9a76836..ec86bb3 100644
--- a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioRecorder.kt
+++ b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioRecorder.kt
@@ -123,6 +123,23 @@ class AudioRecorder(
     startTime = System.currentTimeMillis()
     isRecording = true
     isPaused = false
+
+    if (useForegroundService) {
+      serviceConnection.recordingServiceBinder?.service?.updateNotification(false)
+    }
+
+    appContext?.mainQueue?.launch {
+      emit(
+        RECORDING_STATUS_UPDATE,
+        mapOf(
+          "id" to id,
+          "isFinished" to false,
+          "isPaused" to false,
+          "hasError" to false,
+          "url" to null
+        )
+      )
+    }
   }
 
   fun recordWithOptions(atTimeSeconds: Double? = null, forDurationSeconds: Double? = null) {
@@ -159,6 +176,23 @@ class AudioRecorder(
     durationAlreadyRecorded = getAudioRecorderDurationMillis()
     isRecording = false
     isPaused = true
+
+    if (useForegroundService) {
+      serviceConnection.recordingServiceBinder?.service?.updateNotification(true)
+    }
+
+    appContext?.mainQueue?.launch {
+      emit(
+        RECORDING_STATUS_UPDATE,
+        mapOf(
+          "id" to id,
+          "isFinished" to false,
+          "isPaused" to true,
+          "hasError" to false,
+          "url" to null
+        )
+      )
+    }
   }
 
   fun stopRecording(): Bundle {
diff --git a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/AudioRecordingService.kt b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/AudioRecordingService.kt
index 2c92303..372b357 100644
--- a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/AudioRecordingService.kt
+++ b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/AudioRecordingService.kt
@@ -28,6 +28,9 @@ class AudioRecordingService : Service() {
   @Volatile
   private var isRunningForeground = false
 
+  @Volatile
+  private var isPaused = false
+
   private var weakContext: WeakReference<AppContext>? = null
   var appContext: AppContext?
     get() = weakContext?.get()
@@ -48,6 +51,12 @@ class AudioRecordingService : Service() {
           startForegroundWithNotification()
         }
       }
+      ACTION_PAUSE_RECORDING -> {
+        pauseRecordingFromService()
+      }
+      ACTION_RESUME_RECORDING -> {
+        resumeRecordingFromService()
+      }
       ACTION_STOP_RECORDING -> {
         stopRecordingAndService()
       }
@@ -61,21 +70,37 @@ class AudioRecordingService : Service() {
       if (notificationManager.getNotificationChannel(CHANNEL_ID) == null) {
         val channel = NotificationChannel(
           CHANNEL_ID,
-          "Audio Recording",
-          NotificationManager.IMPORTANCE_LOW
+          "Voice Recording",
+          NotificationManager.IMPORTANCE_DEFAULT
         ).apply {
-          description = "Shows when audio is being recorded in the background"
+          description = "Displays recording indicator and background controls"
           setShowBadge(false)
           enableVibration(false)
+          vibrationPattern = null
           enableLights(false)
           setSound(null, null)
+          lockscreenVisibility = Notification.VISIBILITY_PUBLIC
         }
         notificationManager.createNotificationChannel(channel)
       }
     }
   }
 
-  private fun buildNotification(): Notification {
+  private fun getSmallIconResId(): Int {
+    var id = resources.getIdentifier("notification_icon", "drawable", packageName)
+    if (id == 0) {
+      id = resources.getIdentifier("ic_launcher_monochrome", "drawable", packageName)
+    }
+    if (id == 0) {
+      id = applicationInfo.icon
+    }
+    if (id == 0) {
+      id = android.R.drawable.presence_audio_online
+    }
+    return id
+  }
+
+  private fun buildNotification(paused: Boolean = false): Notification {
     val stopIntent = Intent(this, AudioRecordingService::class.java).apply {
       action = ACTION_STOP_RECORDING
     }
@@ -86,6 +111,16 @@ class AudioRecordingService : Service() {
       PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE
     )
 
+    val toggleIntent = Intent(this, AudioRecordingService::class.java).apply {
+      action = if (paused) ACTION_RESUME_RECORDING else ACTION_PAUSE_RECORDING
+    }
+    val togglePendingIntent = PendingIntent.getService(
+      this,
+      1,
+      toggleIntent,
+      PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE
+    )
+
     val contentIntent = packageManager.getLaunchIntentForPackage(packageName)?.let { appIntent ->
       PendingIntent.getActivity(
         this,
@@ -95,26 +130,53 @@ class AudioRecordingService : Service() {
       )
     }
 
-    return NotificationCompat.Builder(this, CHANNEL_ID)
-      .setContentTitle("Recording audio")
-      .setContentText("Tap to return to app")
-      .setSmallIcon(android.R.drawable.ic_btn_speak_now)
+    val title = if (paused) "Recording Paused" else "Recording Voice Journal"
+    val content = if (paused) "Tap Resume to continue recording" else "Recording audio in background • Tap to open"
+
+    val builder = NotificationCompat.Builder(this, CHANNEL_ID)
+      .setContentTitle(title)
+      .setContentText(content)
+      .setSmallIcon(getSmallIconResId())
+      .setColor(0xFFE53935.toInt()) // Red accent color for recording
+      .setColorized(true)
       .setOngoing(true)
       .setContentIntent(contentIntent)
-      .addAction(
-        android.R.drawable.ic_delete,
-        "Stop",
-        stopPendingIntent
-      )
       .setCategory(NotificationCompat.CATEGORY_SERVICE)
+      .setPriority(NotificationCompat.PRIORITY_DEFAULT)
       .setSilent(true)
-      .setShowWhen(false)
-      .setVisibility(NotificationCompat.VISIBILITY_SECRET) // Hide from lock screen on secure devices
-      .build()
+      .setShowWhen(true)
+      .setVisibility(NotificationCompat.VISIBILITY_PUBLIC)
+      .setStyle(
+        NotificationCompat.BigTextStyle()
+          .setBigContentTitle(title)
+          .bigText(content)
+      )
+
+    if (paused) {
+      builder.addAction(
+        androidx.media3.session.R.drawable.media3_icon_play,
+        "Resume",
+        togglePendingIntent
+      )
+    } else {
+      builder.addAction(
+        androidx.media3.session.R.drawable.media3_icon_pause,
+        "Pause",
+        togglePendingIntent
+      )
+    }
+
+    builder.addAction(
+      android.R.drawable.ic_menu_close_clear_cancel,
+      "Stop",
+      stopPendingIntent
+    )
+
+    return builder.build()
   }
 
   private fun startForegroundWithNotification() {
-    val notification = buildNotification()
+    val notification = buildNotification(isPaused)
 
     try {
       if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.R) {
@@ -141,6 +203,38 @@ class AudioRecordingService : Service() {
     isRunningForeground = false
   }
 
+  fun updateNotification(paused: Boolean) {
+    isPaused = paused
+    if (isRunningForeground) {
+      val notificationManager = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
+      notificationManager.notify(notificationId, buildNotification(paused))
+    }
+  }
+
+  private fun pauseRecordingFromService() {
+    synchronized(recorderLock) {
+      activeRecorders.forEach { weakRef ->
+        val recorder = weakRef.get()
+        if (recorder != null && recorder.isRecording) {
+          recorder.pauseRecording()
+        }
+      }
+    }
+    updateNotification(paused = true)
+  }
+
+  private fun resumeRecordingFromService() {
+    synchronized(recorderLock) {
+      activeRecorders.forEach { weakRef ->
+        val recorder = weakRef.get()
+        if (recorder != null && recorder.isPaused) {
+          recorder.record()
+        }
+      }
+    }
+    updateNotification(paused = false)
+  }
+
   fun registerRecorder(recorder: AudioRecorder) {
     synchronized(recorderLock) {
       activeRecorders.removeAll { it.get() == null }
@@ -151,8 +245,11 @@ class AudioRecordingService : Service() {
 
       activeRecorders.add(WeakReference(recorder))
 
+      isPaused = recorder.isPaused
       if (activeRecorders.size == 1 || !isRunningForeground) {
         startForegroundWithNotification()
+      } else {
+        updateNotification(isPaused)
       }
     }
   }
@@ -194,9 +291,11 @@ class AudioRecordingService : Service() {
   }
 
   companion object {
-    private const val CHANNEL_ID = "expo_audio_recording_channel"
+    private const val CHANNEL_ID = "voice_journal_recording_v2"
     private const val NOTIFICATION_ID = 2001
     internal const val ACTION_START_RECORDING = "expo.modules.audio.action.START_RECORDING"
+    internal const val ACTION_PAUSE_RECORDING = "expo.modules.audio.action.PAUSE_RECORDING"
+    internal const val ACTION_RESUME_RECORDING = "expo.modules.audio.action.RESUME_RECORDING"
     internal const val ACTION_STOP_RECORDING = "expo.modules.audio.action.STOP_RECORDING"
   }
 }
